#include<iostream>
//#include <bits/stdc++.h> 
#include<vector>
int main(){

} 
/*
void ans(vector<int>nums,vector<int> output,int index, vector<vector<int>>&ans){
    //base case if we finish index then get out
    if(index>=nums.size()){
        ans.push_back(output);
        return ;
    }

    //exclude wala case
    solve(nums,output,index+1,ans);

    //include
    int element = nums[index];
    output.push_back(element);
    solve(nums,output,index+1,ans);

}

vector<vector<int>> pwset(vector<int>nums)
{   
    vector<vector<int>> ans;
    vector<int> output;
    int index = 0;
    solve(nums,output,index,ans);
    return ans;
     
}  
*/